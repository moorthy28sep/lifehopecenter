
  # Update banner design elements

  This is a code bundle for Update banner design elements. The original project is available at https://www.figma.com/design/hX8aQrdr22wQw3IpMa7XMA/Update-banner-design-elements.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  